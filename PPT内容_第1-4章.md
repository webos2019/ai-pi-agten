# Pi Agent — Agent Infra 初赛方案 PPT 内容

## 第 1 页：封面

```
GOAI 世界人工智能开源大赛
初赛方案 PPT

Pi Agent — 多 Agent 协同智能助手平台
Datawhale · 参赛选手
Agent Infra 新智基座 初赛
```

---

## 第 2 页：P0 · 一页纸速览

| 板块 | 内容 |
|------|------|
| **项目名称** | Pi Agent — 多 Agent 协同智能助手平台 |
| **问题与场景** | 个人用户在日常办公和学习中需要跨多个信息源（网页、文件、股票、公众号、GitHub 等）获取和处理信息，现有聊天机器人无法多步推理、不能跨工具协同、缺乏长期记忆 |
| **核心解决方案** | 基于 pi.dev 风格的双层 Agent Loop 架构，构建"父 Agent + 3 类子 Agent + 7 个 Skill + 19 个工具"的协同体系，配合三层短期记忆 + 向量长期记忆 |
| **创新点与差异化** | ① 受控 Agent 运行时（/tasklist 显式入口 + 确定性质量门 + 自动修正）<br>② 流式插话机制（Steer）— 用户可中途改变方向<br>③ Skill 文件化懒加载（借鉴 Pi progressive disclosure） |
| **开放 / 复用价值** | Skill 以 Markdown 文件定义，新增能力只需增改文件；Agent Loop 钩子化设计可复用；向量记忆系统可独立部署 |
| **当前进展** | 已完成可运行的 Web 应用（FastAPI + React），支持多会话、流式输出、工具调用、子代理委托、长期记忆 |

---

## 第 3 页：目录

```
1  场景与价值                          5  工程落地、运行验证与安全可审计
2  方案总览                            6  开放 / 开源计划
3  多 Agent 协同设计                     7  落地计划与进展
4  Skills 工具体系                      8  Demo 视频（如有）
```

---

## 第 4 页：第一章 · 场景与价值（标题）

**对应评分维度**：场景价值与行业可复制性 **25%**

---

## 第 5 页：第一章 · 场景与价值（内容）

### 目标用户

| 用户画像 | 核心痛点 |
|---------|---------|
| **知识工作者**（研究员、分析师） | 需要跨多个信息源收集数据，手动切换工具效率低 |
| **个人投资者** | 查行情、看技术指标、读公众号分析，工具分散 |
| **学生 / 学习者** | 搜索资料、读论文 PDF、看 YouTube 视频，缺乏统一入口 |
| **内容创作者** | 需要搜集素材、整理结构、撰写初稿，重复劳动多 |

### 真实场景

**场景 1：投资研究**
> 用户问"帮我看看共进股份 603118 的技术面和 ESG"
> → Pi Agent 调用 stock_analysis 工具获取 K 线 → 计算 7 因子（MACD/RSI/KDJ）→ 获取财务数据 → ESG 评分 → 综合结论

**场景 2：学习研究**
> 用户问"帮我读一下这个 Datawhale 教程讲了什么"
> → Pi Agent 调用 web_fetch 抓取网页 → 提取结构化内容 → 生成摘要和学习路径

**场景 3：跨源信息整合**
> 用户问"帮我总结这篇公众号文章的要点，再搜一下相关资料"
> → wechat_article 提取文章 → 委托 research 子代理搜索关联信息 → 综合输出

### 可量化价值

| 指标 | 传统方式 | Pi Agent |
|------|---------|----------|
| 跨源信息收集 | 15-30 分钟 | 30 秒内完成 |
| 股票技术分析 | 手动查 + 计算 | 自动完成，含 ESG 评分 |
| 上下文保持 | 每次对话重新描述 | 三层记忆 + 长期记忆自动召回 |

### 行业可复制性

- **金融领域**：替换数据源用于基金/债券分析
- **教育领域**：Skill 组合适配不同学科研究助手
- **企业服务**：Agent Loop + 子代理架构用于运维故障排查
- **内容平台**：wechat_article 工具模式复用于知乎、小红书

### 创新点与差异化

1. **受控 Agent 运行时**：不是"什么都会一点"的聊天机器人，而是有显式入口、确定性质量门、自动修正的工程化 Agent
2. **流式插话（Steer）**：借鉴 pi.dev，用户可在 Agent 执行过程中改变方向
3. **文件化 Skill 系统**：新增能力只需在 skills/ 目录加一个 .md 文件

---

## 第 6 页：第二章 · 方案总览（标题）

---

## 第 7 页：第二章 · 方案总览（内容）

### 架构概览

```
┌─────────────────────────────────────────────────────────────┐
│                      React Frontend                         │
│  对话界面 | 股票分析面板 | 会话侧栏 | 富文本编辑器（@命令）  │
└─────────────────────────────┬───────────────────────────────┘
                              │ NDJSON Stream
┌─────────────────────────────┼───────────────────────────────┐
│                    FastAPI Backend                           │
│  Chat Orchestrator (编排器)                                     │
│  ├─ Pi Agent 分支 (/tasklist 入口 + 质量门 + 修正)            │
│  └─ 普通聊天分支 (Agent Loop 双层循环)                          │
│       │                                                          │
│       ├─ 主 Agent (19 个工具)                                   │
│       │     └─ 委托子代理                                       │
│       │           ├─ research 子代理 (信息检索)                 │
│       │           ├─ analysis 子代理 (数据分析)                 │
│       │           └─ writer 子代理 (内容创作)                   │
│       │                                                          │
│       ├─ Skill Registry (7 Skills: utility/reader/...)         │
│       └─ Tool Registry (19 Tools: calculator/web_search/...)    │
│                                                                  │
│  ├─ ThreadState (短期记忆: recent + summary + pinned)           │
│  ├─ UserMemory (长期记忆: 向量搜索)                              │
│  └─ Steer Queue (流式插话)                                        │
└─────────────────────────────┬───────────────────────────────┘
                              │ DeepSeek API (OpenAI 兼容)
```

### 端到端流程

```
用户输入 → Chat Orchestrator
    ├─ 命中 /tasklist? 
    │   ├─ 是 → Pi Agent (7 步状态机: 读取方案 → 提取 → 草稿 → 质量门 → 修正 → 输出)
    │   └─ 否 → Agent Loop
    │        └─ LLM 推理 → 工具调用(并发)
    │             ├─ 直接执行 (19 个工具)
    │             └─ 子代理委托 (research/analysis/writer)
    │        → 工具结果注入 → LLM 继续推理
    │        → Steer 检查 (用户可中途插话)
    │        → 最终回答 → 流式输出
    → 写入 ThreadState + UserMemory
```

### 关键技术选型

| 选型 | 理由 |
|------|------|
| DeepSeek API | OpenAI 兼容、中文能力强、性价比高 |
| FastAPI + asyncio | 原生异步支持流式输出 |
| NDJSON 流式协议 | 比 SSE 更灵活，支持多类型 chunk |
| DuckDB 持久化 | 嵌入式、零运维、支持向量化查询 |
| React + TypeScript | 类型安全、组件化、生态成熟 |